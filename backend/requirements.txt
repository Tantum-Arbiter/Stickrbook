fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
httpx>=0.26.0
websockets>=12.0
python-multipart>=0.0.6
ulid-py>=1.1.0
aiofiles>=23.2.0
sse-starlette>=1.8.0
Pillow>=10.0.0

# Database
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.19.0
alembic>=1.13.0
greenlet>=3.0.0  # Required for async SQLAlchemy

# Optional: PostgreSQL support (uncomment for production)
# asyncpg>=0.29.0
# psycopg2-binary>=2.9.9

# Optional: Background removal (heavy dependency, install separately if needed)
# pip install rembg[gpu] for GPU acceleration
# rembg>=2.0.50

# Development/Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
